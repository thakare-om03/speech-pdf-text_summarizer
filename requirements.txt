# Streamlit for the app UI
streamlit

# Hugging Face Transformers for NLP models (summarization, question-answering)
transformers

# PyPDF2 for PDF handling
PyPDF2

# Regular Expressions for text manipulation (standard library, no install needed)
# re

# Pandas for data manipulation, especially for CSV news articles
pandas

# Google Generative AI for model-based content generation
google-generativeai
google-auth  # Needed for Google API authentication

# YouTube Transcript API for fetching video transcripts
youtube-transcript-api

# Speech Recognition for converting audio to text
SpeechRecognition

# Textwrap for chunking text (standard library, no install needed)
# textwrap

# Optional: torch if using Hugging Face models that require PyTorch backend
torch
